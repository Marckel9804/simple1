spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db-obi34-kr.vpc-pub-cdb.ntruss.com:3306/ncloud_db
    username: user
    password: "!qjem0421"

  jpa:

    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true

    hibernate:
      ddl-auto: create
      #      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true